#!/usr/bin/env python3
"""ROM Groomer Quick Build Launcher - Interactive build wizard."""

import sys
from pathlib import Path

# Add src to path so we can import romgroomer
sys.path.insert(0, str(Path(__file__).parent / "src"))

from romgroomer.cli.quick import quick

if __name__ == '__main__':
    # Always run in interactive mode
    sys.argv.append('--interactive')
    quick()
