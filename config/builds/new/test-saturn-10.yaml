# Quick Test Build — New Format
# Test a few games from Saturn to verify pipeline works.

name: test-saturn-10
description: "Saturn test build — 10 random games"
target: batocera-pc

recipes:
  - redump-chd     # Saturn is a disc platform → CHD
  - test-10        # Limit to 10 random games (seed 42)

platforms:
  - saturn
