# Quick Test Build - Single Platform
# Tests the complete pipeline with Game Boy only

name: quick-test
description: "Quick test build - GB only"

target: rocknix-r36s
profile: handheld

platforms:
  - gb

output:
  base_path: output/quick-test
  create_directories: true

options:
  dry_run: false
  verbose: true
  parallel: false
  clean_output: true
