"""romfarmer.targets — EMIT phase: TargetProfile loader and TargetEmitter implementations.

Public API:
    targets.profiles.loader   — load config/frontends/*.yaml + config/devices/*.yaml
    targets.emitters.*        — TargetEmitter implementations
"""
